<% if model.errors.any? %>
<div class="error_explanation">
  <h2><%= pluralize(model.errors.count, "error") %> prevented this record from being saved:</h2>
  <ul>
  <% model.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>